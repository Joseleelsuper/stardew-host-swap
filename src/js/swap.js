/**
 * Stardew Valley Host Swap Tool
 * Updated version (2025) to handle ZIP files and modern save structure
 *
 * This file serves as an entry point that imports necessary modules.
 * All modules are in the 'modules' folder.
 */

// Import the main module
import "./modules/main.js";

// Add event listener for when DOM is loaded
document.addEventListener("DOMContentLoaded", function () {});
